import { FormEvent,useState } from 'react'
interface TerminalProps{history:Array<{command:string;output:string}>;onCommand:(command:string)=>void}
export function Terminal({history,onCommand}:TerminalProps){const[input,setInput]=useState('');function submit(e:FormEvent){e.preventDefault();if(!input.trim())return;onCommand(input);setInput('')};return <section className="rounded-xl border border-slate-800 bg-slate-950 p-4 font-mono text-sm shadow-2xl"><div className="mb-3 text-xs uppercase tracking-[0.2em] text-emerald-400">Terminal Simulator</div><div className="h-72 space-y-3 overflow-auto" aria-live="polite"><div className="text-slate-400">Ketik <span className="text-slate-200">git status</span> untuk mulai mengobservasi.</div>{history.map((entry,index)=><div key={`${entry.command}-${index}`}><div className="text-emerald-300">$ {entry.command}</div><pre className="whitespace-pre-wrap text-slate-300">{entry.output}</pre></div>)}</div><form onSubmit={submit} className="mt-3 flex items-center gap-2 border-t border-slate-800 pt-3"><span className="text-emerald-400">$</span><input value={input} onChange={e=>setInput(e.target.value)} className="w-full bg-transparent text-slate-100 outline-none" placeholder="git status" aria-label="Command Git" autoComplete="off"/></form></section>}
